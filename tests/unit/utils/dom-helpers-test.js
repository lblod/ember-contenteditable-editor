import domHelpers from 'dummy/utils/dom-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | dom helpers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = domHelpers();
  assert.ok(result);
});
