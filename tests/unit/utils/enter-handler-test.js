import enterHandler from 'dummy/utils/enter-handler';
import { module, test } from 'qunit';

module('Unit | Utility | enter handler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = enterHandler();
  assert.ok(result);
});
